/**
 * @link https://en.wikipedia.org/wiki/R
 */

// import the lower case character r
import r from "@characters/r";
// transform it into upper case to get the upper case character R
export default r.toUpperCase();
