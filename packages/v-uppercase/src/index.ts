/**
 * @link https://en.wikipedia.org/wiki/V
 */

// import the lower case character v
import v from "@characters/v";
// transform it into upper case to get the upper case character V
export default v.toUpperCase();
