/**
 * @link https://en.wikipedia.org/wiki/H
 */

// import the lower case character h
import h from "@characters/h";
// transform it into upper case to get the upper case character H
export default h.toUpperCase();
