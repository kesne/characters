/**
 * @link https://en.wikipedia.org/wiki/A
 */

// import the lower case character a
import a from "@characters/a";
// transform it into upper case to get the upper case character A
export default a.toUpperCase();
